# Internal Notes

This directory contains maintainer-facing implementation notes, issue writeups, and planning documents that support the active product in [`app/`](../../app).

These files are kept out of the repository root so the public project surface stays focused on the shipped application, documentation, and brand assets.
